# COMPLEX

# Use your script in a Lambda function using Python 3.7 or higher runtime. Make sure the lambda runs on a set schedule daily. No one should be working in the Dev environment past 7pm.
#  (Note: to test you may need to modify the time accordingly so you aren't waiting for 7pm)

import boto3
import json
# print(json.dumps(response, indent=2, default=str))

